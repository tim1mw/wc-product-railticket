<div style='max-width:550px;margin-left:0px;margin-right:auto;'>
    <h1>Move Bookings</h1>
    <table border='1' class='railticket_admintable' style='width:100%;'>
    {{# details }}
        <tr><th width='30%' style='text-align:right'>{{item}}</th><td class='railticket_meta'>{{{value}}}</td></tr>
    {{/ details }}
    </table>
    <br />

    <table border='1' class='railticket_admintable' style='width:100%;'>
        <tr>
            <th>Date</th>
            <td style='text-align:center'>{{date}}</td>
        </tr><tr>
            <th></th>
            <th>From</th>
            <th>To</th>
            <th>Departure</th>
            <th>Seats/Bays</th>
        </tr>
        {{#tripdata}}
        <tr>
            <th>Trip {{num}}</th>
            <td style='text-align:center'>{{from}}</td>
            <td style='text-align:center'>{{to}}</td>
            <td style='text-align:center'>{{dep}}</td>
            <td style='text-align:center'>{{seats}}</td>
        </tr>
        {{/tripdata}}
    </table>

    <h3>Set new trip details</h3>
    <div id="pleasewait" class="railticket_loading"></div>
    <div id='railticket_movebooking'></div>

    <p>Note: Train capacites shown exclude the seats used for the order being moved,
    no actual changes will be made until the commit button is clicked.</p>
    <script type='text/javascript'>
        var orderid = '{{orderid}}';
        var ajaxurl = '{{ajaxurl}}';
        var defaultData = {{{defaultdata}}};
    </script>

    <h3>Override Bay/Seat Allocations</h3>
    <p>Note: bays or seats are automatically allocated when trip details are updated, so this must always be done after trip details are changed. Requested seats represents the number of seats required for the booked tickets, which doesn't have to match the actual space allocated.</p>

    <div id='railticket_overridebays'></div>

    <h3>Edit Order Notes</h3>
    <form action="" method="post">
        <input type="hidden" name="action" value="saveordernote">
        <input type="hidden" name="orderid" value="{{orderid}}">
        <textarea name='notes' cols='40' rows='5'>{{notes}}</textarea>
        <input type="submit" value="Save Notes">
    </form>

    <hr />

    <form action="" method="post" id='railticket_backtoview'>
        <input type="hidden" name="action" value="showorder">
        <input type="hidden" name="orderid" value="{{orderid}}">
        <input type="submit" value="Back to {{orderid}} full order view">
    </form>

</div>
